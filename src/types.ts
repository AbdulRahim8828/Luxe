import { ComponentType } from 'react';

export interface BlogPostData {
  slug: string;
  title: string;
  image: string;
  date: string;
  author: string;
  category: string;
  readTime: string;
  description: string;
  keywords: string;
  content: string;
}
